/// <reference types="cypress" />

describe("Test sobre TIENDA CLARO", { tags: "@claro" }, () => {
  beforeEach(() => {
    cy.visit("https://tienda.claro.com.ar/");
  });

  it("Ejemplo", { tags: "@plp" }, () => {
    //Ejemplo
    //Solucione lo que migue me comento en github
  });
});
